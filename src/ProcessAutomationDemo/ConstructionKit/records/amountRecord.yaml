$schema: https://schemas.meshmakers.cloud/construction-kit-elements.schema.json
records:
  - recordId: AmountRecord
    attributes:
      - id: ${this}/AmountValue
        name: Value
      - id: ${this}/Currency
        name: Currency
